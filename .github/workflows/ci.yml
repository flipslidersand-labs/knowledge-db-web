name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    uses: flipslidersand-labs/qa-workflows/.github/workflows/node-test.yml@main
    with:
      node-version: "20"
      package-manager: "npm"
